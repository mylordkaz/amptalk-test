# Test Environment Variables
# This file is used during test execution

# Database (mocked in tests, but required for Prisma client initialization)
DATABASE_URL="postgresql://test:test@localhost:5432/test_db?sslmode=disable"

# JWT Authentication
JWT_SECRET="test-jwt-secret-key-for-testing-only-do-not-use-in-production"
JWT_EXPIRES_IN="1h"

# Cloudinary (mocked in tests)
CLOUDINARY_CLOUD_NAME="test-cloud"
CLOUDINARY_API_KEY="test-api-key"
CLOUDINARY_API_SECRET="test-api-secret"

# Server
PORT=3001
NODE_ENV="test"

# Frontend URL (for CORS)
FRONTEND_URL="http://localhost:5173"
